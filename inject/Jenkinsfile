// Jenkinsfile for Inject
pipeline {
  agent {
    kubernetes {
      cloud "openshift"
      containerTemplate {
        name 'jnlp'
        image "hseeberger/scala-sbt"
      }
    }
  }
  options { retry(3) }
  stages {
    stage('INJECT Build') {
      steps {
        container('jnlp') {
          dir('inject') {
            sh "sbt clean assembly dockerFileTask"
            sh "rm -R ./docker"
            sh "mv target/docker ."
            sh "ls -als"
          }
        }
      }
    }
  }
}