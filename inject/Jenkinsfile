// Jenkinsfile for Inject
// @See https://akomljen.com/set-up-a-jenkins-ci-cd-pipeline-with-kubernetes/

def label = "jenkins-pod2"
podTemplate(label: label, cloud: "openshift", containers: [
  containerTemplate(name: 'sbt', image: 'hseeberger/scala-sbt', command: 'cat', ttyEnabled: true)
]) {
  node(label) {
    stage('Build') {
      container('sbt') {
        sh "sbt version"
      }
    }
  }
}
