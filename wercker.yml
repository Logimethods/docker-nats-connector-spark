box: hseeberger/scala-sbt
build:
  steps:
    - script:
        name: build docker
        code: |
          pushd shell
          sbt clean
          popd
          ls -als ./*/target/docker/
          echo "++++++++++++++++++++++++++++++++++++++"
          pushd shell
          sbt clean assembly dockerFileTask
          popd
          ls -als ./*/target/docker/


buildX:
  steps:
    - script:
        name: build docker
        code: |
          pushd app
          sbt clean docker
          popd
          pushd inject
          sbt clean docker
          popd
          pushd monitor
          sbt clean docker
          popd
          pushd shell
          sbt clean docker
          popd
          ls ./*/target/docker/
